# ===== 框架全局设置 =====
# 修改后自动热加载，无需重启

# HTTP 服务器
server:
  host: "0.0.0.0"
  port: 5201

# Web 面板
web:
  access_token: "admin"                    # URL 参数验证令牌
  admin_password: "admin"                  # 管理员密码
  framework_name: "Elaina"                 # 框架名称
  favicon_url: ""                          # 网页图标 URL

# OneBot 协议
onebot:
  access_token: ""                         # 默认鉴权 token（连接未单独配置时使用）
  secret: ""                               # 默认 HTTP 上报签名密钥（可选）
  # 网络连接（在「网络配置」页面点「新建」可视化管理，支持多条，默认不预置任何连接）
  # 主服务端口 (server.port) 始终内置 /OneBotv11 反向 WS 入口, 可直接用作反向 WS 连接
  # type: ws_reverse(反向WS) / ws_forward(正向WS) / http_server(HTTP上报) / http_client(HTTP调用)
  # 反向 WS / HTTP 上报可设独立监听 host:port (容器部署), 与主端口相同则复用主服务
  connections: []

# 主人配置
owner:
  ids:                                     # 主人 QQ 号列表
    - ""

# 日志配置
logging:
  dir: "log"                               # 日志根目录
  insert_interval: 2                       # 批量写入间隔(秒)
  retention_days: 30                       # 日志保留天数
  wal_mode: true                           # SQLite WAL模式

# 依赖管理
pip:
  auto_install: true                       # 自动安装模块/插件依赖
  mirror: ""                               # pip 镜像源
